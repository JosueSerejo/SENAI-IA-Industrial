### Observações – Laboratório 3.1

O conjunto de dados foi importado com sucesso a partir do repositório UCI Machine Learning Repository, utilizando um arquivo compactado no formato `.zip`. Após a extração, os dados foram carregados em um DataFrame do Pandas a partir de um arquivo `.arff`.

A verificação inicial mostrou que o dataset possui múltiplas variáveis numéricas relacionadas a medições biomecânicas da coluna vertebral, além de uma variável categórica que representa a classificação das amostras.

Nesta etapa, não foi realizado nenhum tratamento ou transformação dos dados, uma vez que o objetivo do Laboratório 3.1 é apenas o carregamento e a inspeção inicial da base. Observou-se que a variável categórica encontra-se em formato `bytes`, o que poderá exigir conversão ou codificação nas etapas posteriores de preparação dos dados.